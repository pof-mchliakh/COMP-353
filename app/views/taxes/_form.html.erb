<%= form_for @tax, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :start_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.datetime_select :start_date, :class => 'datetime_select' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :end_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.datetime_select :end_date, :class => 'datetime_select' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :percentage, :class => 'control-label' %>
    <div class="controls">
        <%= f.text_field :percentage, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :tax_type_id, :class => 'control-label' %>
    <div class="controls">
      <%= collection_select(:tax, :tax_type_id, TaxType.all, :id, :name) %> 
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                taxes_path, :class => 'btn' %>
  </div>
<% end %>
